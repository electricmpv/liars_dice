{
  /* Base configuration. Do not edit this field. */
  "extends": "./temp/tsconfig.cocos.json",

  /* Add your custom configuration here. */
  "compilerOptions": {
    "strict": true,
    "lib": ["ES2018", "DOM"],
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "skipLibCheck": true,
    "baseUrl": "./", // Specify the base directory for module resolution
    "paths": {
      "events": ["./node_modules/events/events.js"], // Map 'events' to the browser polyfill
      "colyseus.js": ["./node_modules/colyseus.js/dist/esm/index.mjs"] // Point directly to the ESM build
      // Removed "shared/*" path mapping
    }
  }
}
